import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#EC058E',
    },
    secondary: {
      main: '#62BBC1',
    },
  },
})

export default theme
