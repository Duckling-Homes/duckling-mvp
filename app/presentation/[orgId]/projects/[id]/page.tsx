'use client'

import { Container } from '@/components/Container'

const ShareablePresentation = () => {
  return (
    <Container publicRoute={true}>
      <div>presentation</div>
    </Container>
  )
}

export default ShareablePresentation
